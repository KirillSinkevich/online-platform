# Enable strict peer dependencies
strict-peer-dependencies=false

# Hoist all dependencies to node_modules (for compatibility with tools that don't support pnpm)
public-hoist-pattern[]=*

# Auto install peers
auto-install-peers=true

# Store location
store-dir=.pnpm-store

# Node linker - use hoisted for better compatibility
node-linker=hoisted
